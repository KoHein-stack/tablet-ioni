<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

  <!-- MAIN APP THEME -->
  <style name="Theme.TabletIonic" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="android:windowFullscreen">false</item>
    <item name="android:windowDrawsSystemBarBackgrounds">true</item>
  </style>

  <!-- SPLASH SCREEN THEME (Android 12+) -->
  <style name="Theme.SplashScreen" parent="Theme.SplashScreen" tools:ignore="ResourceCycle">
    <!-- Background color of splash screen -->
    <item name="android:windowSplashScreenBackground">@color/white</item>

    <!-- Animated icon for splash -->
    <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

    <!-- Theme to apply after splash -->
  </style>

</resources>
